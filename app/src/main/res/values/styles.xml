<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:windowTranslucentStatus">true</item>
    </style>

    <style name="BaseAppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryDark">@color/gray</item>
        <item name="colorAccent">@color/dark_gray</item>
        <item name="titleTextColor">@color/dark_red</item>
    </style>

    <style name="SearchAppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryDark">@color/gray</item>
        <item name="colorAccent">@color/dark_gray</item>
        <item name="titleTextColor">@color/dark_red</item>
    </style>
  
    <style name="NewRecipeAppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryDark">@color/gray</item>
        <item name="colorAccent">@color/dark_gray</item>
        <item name="titleTextColor">@color/dark_red</item>
    </style>

    <style name="AppTheme.NoTitle">
        <item name="android:windowNoTitle">true</item>

        <!-- Support library compatibility -->
        <item name="windowActionBar">false</item>
    </style>

    <style name="MyTheme" parent="AppTheme">
        <item name="android:windowBackground">@android:color/white</item>
    </style>

    <style name="fontForStepTitle">
        <item name="android:textStyle">bold</item>
        <item name="android:fontFamily">@font/average_sans</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">20sp</item>
        <item name="android:shadowColor">@color/black</item>
        <item name="android:shadowDx">1</item>
        <item name="android:shadowDy">1</item>
        <item name="android:shadowRadius">2</item>

    </style>

    <style name="fontForStep">
        <item name="android:fontFamily">@font/average_sans</item>
        <item name="android:padding">15dp</item>

    </style>


</resources>
